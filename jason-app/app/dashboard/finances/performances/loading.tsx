// Cf. commentaire dans finances/revenus/loading.tsx — même besoin pour cet
// onglet (route différente de /dashboard/performances, son loading.tsx ne
// s'applique pas ici).
import Loading from '@/app/dashboard/performances/loading'

export default Loading
