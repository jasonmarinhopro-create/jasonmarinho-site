// Cf. commentaire dans finances/revenus/loading.tsx — même besoin pour cet
// onglet (route différente de /dashboard/encaissements, son loading.tsx ne
// s'applique pas ici).
import Loading from '@/app/dashboard/encaissements/loading'

export default Loading
